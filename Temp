spring:
  application:
    name: LoginService

  profiles:
    active: dev
  main:
    web-application-type: reactive

  gateway:
    routes:
      - id: product-service
        uri: http://product-service:8081
        predicates:
          - Path=/Product/**
      - id: fincore-service
        uri: http://fincore-service:8089
        predicates:
          - Path=/Fincore/**
  kubernetes:
    discovery:
      enabled: false

# ========== DB Details ==========
  datasource:
    url: jdbc:oracle:thin:@10.177.103.192:1523/fincorepdb1
    username: fincore
    password: Password#1234
    driver-class-name: oracle.jdbc.OracleDriver

server:
  port: 8085

# ========== LDAP CONFIGURATION (UPDATED) ==========
  ldap:
    # CRITICAL CHANGE: Use Hostname + Port 3269 (Global Catalog)
    # This matches what we force in Kubernetes via HostAliases
    urls: ldaps://uatrootdc1.uatad.sbi:3269
    base: DC=UATAD,DC=SBI
    domain: UATAD.SBI
    # Add username/password placeholders for the Service Account (Bind User)
    # You will fill these via Env Vars later when you get them from AD Team
    username: "" 
    password: ""

# ========== SECURITY ==========
security:
  jwt:
    mode: hmac
    hmac-base64-secret: UG5ldW1vbm91bHRyYW1pY3Jvc2NvcGljc2lsaWNvdm9sY2Fub2Nvbmlvc2lz
    rsa-public: ""
    ttl-seconds: 1800
    bypass-paths: /auth/login,/auth/logout,/auth/check-user

  # LDAP TRUSTSTORE (Supports Local & K8s paths via Env Var override)
  ldap:
    # Default to local path for dev, override in K8s via LDAP_TRUSTSTORE_PATH env var
    truststore-path: ${LDAP_TRUSTSTORE_PATH:file:C:/fincore/secrets/ad-truststore.jks}
    truststore-password: ${LDAP_TRUSTSTORE_PASSWORD:changeit}

# ========== Redis ==========
redis:
  enabled: true
  data:
    redis:
      host: localhost
      port: 6379
      database: 0

# ========== Actuator ==========
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

# ========== Logging ==========
logging:
  pattern:
    console: "%d{yyyy-MM-dd :: HH:mm:ss.SSS ||} %highlight(%-5level:: %file: | %line |){ERROR=bold red, WARN=yellow, INFO=white, DEBUG=green, TRACE=green} :: %msg%n"


