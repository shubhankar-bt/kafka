
Error starting at line : 106 in command -
BEGIN
  DBMS_SCHEDULER.CREATE_JOB (
    job_name        => 'SYNC_PERMISSIONS_TO_AI_HELP',
    job_type        => 'PLSQL_BLOCK',
    job_action      => '
      BEGIN
        -- STEP 1: Update URLs and Active status for existing screens. 
        -- (This safely ignores question/answer text to protect Admin edits)
        UPDATE help_question_master hqm
        SET (action_link, is_active, screen_name) = (
            SELECT MENU_URL, ''Y'', MENU_TITLE
            FROM permissions p
            WHERE p.MENU_ID = hqm.permission_id
        )
        WHERE EXISTS (
            SELECT 1 FROM permissions p WHERE p.MENU_ID = hqm.permission_id
        );

        -- STEP 2: Generate the 4-Angle Multiplier for completely NEW screens
        INSERT INTO help_question_master (
            question_id, screen_name, question_text, answer_content, 
            permission_id, action_link, action_label, pro_tip, is_active
        )
        WITH new_screens AS (
            SELECT MENU_ID, MENU_TITLE, MENU_SUBMENU, MENU_ACTION, MENU_DESCRIPTION, MENU_URL
            FROM permissions
            WHERE MENU_URL IS NOT NULL
              -- Only select screens that are NOT in the help table yet
              AND MENU_ID NOT IN (SELECT NVL(permission_id, 0) FROM help_question_master)
        ),
        question_types AS (
            SELECT 1 AS q_type FROM dual UNION ALL
            SELECT 2 AS q_type FROM dual UNION ALL
            SELECT 3 AS q_type FROM dual UNION ALL
            SELECT 4 AS q_type FROM dual
        )
        SELECT 
            FINCORE.ISEQ$$_78061.nextval, 
            s.MENU_TITLE,

            CASE q.q_type
                WHEN 1 THEN ''How do I access, find, or navigate to the '' || s.MENU_SUBMENU || '' screen?''
                WHEN 2 THEN ''What is the purpose of '' || s.MENU_SUBMENU || ''? What can I do there?''
                WHEN 3 THEN 
                    CASE 
                        WHEN UPPER(s.MENU_ACTION) LIKE ''%APPROVE%'' THEN ''How do I review, approve, or reject pending requests in '' || s.MENU_SUBMENU || ''?''
                        WHEN UPPER(s.MENU_SUBMENU) LIKE ''%REPORT%'' THEN ''How do I view, generate, or download the '' || s.MENU_SUBMENU || ''?''
                        WHEN UPPER(s.MENU_ACTION) LIKE ''%CREATE%'' OR UPPER(s.MENU_ACTION) LIKE ''%MODIFY%'' THEN ''How do I manage, create, or update records for '' || s.MENU_SUBMENU || ''?''
                        ELSE ''How do I view the details and records in '' || s.MENU_SUBMENU || ''?''
                    END
                WHEN 4 THEN 
                    CASE 
                        WHEN UPPER(s.MENU_ACTION) LIKE ''%CANCEL%'' AND UPPER(s.MENU_ACTION) NOT LIKE ''%APPROVE%'' THEN ''How do I cancel a pending '' || s.MENU_SUBMENU || '' request that I created?''
                        WHEN UPPER(s.MENU_ACTION) LIKE ''%APPROVE%'' THEN ''I submitted a request for '' || s.MENU_SUBMENU || ''. Can I approve it myself or when will it be approved?''
                        ELSE ''Why can I not access or perform actions on the '' || s.MENU_SUBMENU || '' screen?''
                    END
            END,

            CASE q.q_type
                WHEN 1 THEN ''To access this feature, navigate to the main **'' || s.MENU_TITLE || ''** module from the sidebar, then click on **'' || s.MENU_SUBMENU || ''**.''
                WHEN 2 THEN ''The **'' || s.MENU_SUBMENU || ''** screen is used to: '' || NVL(s.MENU_DESCRIPTION, ''manage related data and workflows'') || ''. Your available actions ('' || REPLACE(s.MENU_ACTION, ''|'', '', '') || '') depend on your assigned role.''
                WHEN 3 THEN 
                    CASE 
                        WHEN UPPER(s.MENU_ACTION) LIKE ''%APPROVE%'' THEN ''Navigate to '' || s.MENU_TITLE || '' -> '' || s.MENU_SUBMENU || ''. From here, Checkers can review pending items submitted by Makers and take Approve or Reject actions. Rejections require a mandatory remark.''
                        WHEN UPPER(s.MENU_SUBMENU) LIKE ''%REPORT%'' THEN ''Navigate to '' || s.MENU_TITLE || '' -> '' || s.MENU_SUBMENU || ''. You can apply date ranges and relevant filters before downloading or extracting the data.''
                        ELSE ''Navigate to '' || s.MENU_TITLE || '' -> '' || s.MENU_SUBMENU || '' to perform your authorized actions. You can initiate new requests or modify existing records, which will then be sent to a Checker for approval.''
                    END
                WHEN 4 THEN 
                    CASE 
                        WHEN UPPER(s.MENU_ACTION) LIKE ''%CANCEL%'' AND UPPER(s.MENU_ACTION) NOT LIKE ''%APPROVE%'' THEN ''If you initiated a request and need to recall it, navigate to the '' || s.MENU_SUBMENU || '' screen. You can only cancel requests that are still in a "Pending" state. Once a Checker processes it, it cannot be cancelled.''
                        WHEN UPPER(s.MENU_ACTION) LIKE ''%APPROVE%'' THEN ''FinCore enforces a strict **Maker-Checker** policy. You cannot approve your own requests, even if your role has approval rights. Another authorized user must review your submission in their pending queue.''
                        ELSE ''Access to this screen requires specific permissions. If you cannot view it or perform certain actions, your current Role does not have the necessary privileges mapped to it. Please contact the Administrator to request access.''
                    END
            END,

            s.MENU_ID,
            s.MENU_URL, 
            ''View '' || s.MENU_SUBMENU,

            CASE q.q_type
                WHEN 1 THEN ''Tip: Use the global search bar at the top to jump to this screen instantly.''
                WHEN 2 THEN ''Tip: Check the Action column on the grid to see what operations you are permitted to perform.''
                WHEN 3 THEN 
                    CASE 
                        WHEN UPPER(s.MENU_ACTION) LIKE ''%APPROVE%'' THEN ''Tip: Always verify the Maker''''s attached documents and input details before approving.''
                        WHEN UPPER(s.MENU_SUBMENU) LIKE ''%REPORT%'' THEN ''Tip: Generating reports for large date ranges may take a few moments. Please be patient.''
                        ELSE ''Tip: Ensure all mandatory fields are correctly filled before submitting your request.''
                    END
                WHEN 4 THEN ''Tip: You can check your assigned roles by navigating to your User Profile.''
            END,

            ''Y''
        FROM new_screens s
        CROSS JOIN question_types q;

        COMMIT;
      END;',
    -- Run daily at 1:00 AM
    start_date      => TRUNC(SYSDATE) + 1 + 1/24, 
    repeat_interval => 'FREQ=DAILY; BYHOUR=1; BYMINUTE=0; BYSECOND=0',
    enabled         => TRUE,
    comments        => 'Dynamically multiplies new Fincore screens into 4 distinct AI Help context angles.'
  );
END;
Error report -
ORA-16612: string value too long for attribute "job_action"
ORA-06512: at "SYS.DBMS_ISCHED", line 175
ORA-06512: at "SYS.DBMS_SCHEDULER", line 286
ORA-06512: at line 2
16612. 00000 -  "string value too long for attribute \"%s\""
*Cause:    The string value for the named attribute was too long.
*Action:   Specify a shorter string value.
